# Comparator acceptance judgment

Measured V0 is trustworthy for this explicit new semantic-retrieval scope, not historical metric parity. All60jobs,148200method-query ranks and nine primary V0 metrics audited, maximum AP discrepancy4.44e-16. Audit-only IO repair reduced repeated decompression; raw measurement unchanged.

Candidate incumbents: V0, strongest single fixed allowed control, per-cell retrospective maximum. Select old_centered as strongest single fixed comparator: macro44.325898 versus V043.306736. Per-cell maximum is descriptive and is not a deployable method. This is a baseline selection after measurement, not a revision of the frozen V0 primary test. All original V0 values remain in outputs/metrics_summary.json and all13variants in outputs/baseline_variants.json.

Why: strongest comparator across the fixed equal-cell surface uses only old-gallery geometry and no bridge/new encoder; future methods must beat this control and compare against V0 and centered_residual_2, not merely raw old scores. DTD matched64 V0 has34.754mAP versus old27.046 and centered30.679; centered_residual_2 has35.381. EuroSAT full-new reference52.998 is below centered-old57.973: no guaranteed positive encoder-upgrade headroom.

Limits: previously observed image pools, semantic class retrieval, one encoder pair, local preprocessing variant and float64 scoring on stored feature vectors; no external instance-retrieval confirmation. Intervals cluster queries by class and resample bridge seeds; fixed galleries, not new-domain evidence. Original paper and historical dataset comparisons remain reference-only.
