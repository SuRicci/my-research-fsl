# Independent FSL transfer experiment

Objective: test frozen matched-gallery iLPC benefit on Pets against canonical R2, CS_l2 and support-logistic controls; no new method claim. Null: no improvement over strongest fixed R2/CS_l2/support-logistic controls. Alternative: >=0.5 pp matched one-shot improvement with positive paired intervals. Required stress: equal1876-image DTD gallery and both shots. protocol.json and artifacts/idea/selected_idea.md define immutable contract and gates.

Current step: acquire public Pets archives, verify source checksums, decode/hash identities, create disjoint query/gallery pools, then reuse canonical encoders locally. No outcome-dependent selection. Raw images/features stay in this run's assets directory with ignored binary folders to avoid new worktree copies. Preserve sources in provenance; result metadata is tracked.

Minimal code map: acquire_assets.py and paired evaluator adapter only; frozen ilpcz/reference functions reused. Target2000 paired task fits per method across4cells; graph cache preserved;4CPUworkers/BLAS1 if no pressure. MPS32image batches/6loaders for encoding. Full target arrays plus task predictions, source hashes, timings, paired comparisons and independent validation. Comparator qualification on new Pets scope must precede candidate claim; old7R2/9R4metric rows remain separate history.

Abandon/downgrade: source integrity/access, insufficient class counts, identity leakage, output mismatch, deadline or disk floor. Do not silently drop a cell or change dataset. No data access beyond authorized public assets. After completed measurement: record main experiment, decision, and next evidence or contribution route.

Validation preparation (2026-09-12 21:12 UTC): add audit_outputs.py only; frozen acquisition/evaluator/method/protocol remain unchanged. Asset gate checks hashes, dimensions, identity uniqueness/disjointness and environment provenance. Baseline gate independently recomputes scores-to-label accuracy, all task memberships, seeds and gallery-invariant controls before separate-scope confirmation. Tier main/test; minimum is four complete comparable cells, solid is the prespecified paired gate, maximum is only justified follow-up. Conditional bootstrap uses paired tasks within each of five fixed seeds; it does not estimate uncertainty over all possible image pools.

Baseline handoff2026-09-12 21:25UTC: separate r2-pets-transfer/frozen-v1 confirmed as baseline-0503efcd, five canonical metrics and all four controls preserved. Comparison-ready; no further baseline rerun unless audit finds a discrepancy. Selected idea already fixed, so decision-cf80e7d2 resumes experiment directly. Current deliverable: full2000 iLPC task outputs and independently recomputed gate.

Exit21:40UTC: complete run-cb50d2d5, primary gate failed; decision-ed211b59. Outputs/analysis.json,validation.json and RESULT.json authoritative. Next lightweight no-refit failure report; no target tuning.
