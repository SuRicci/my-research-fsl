# Objective contract — representation qualification

Real objective: robust frozen-encoder FSL accuracy at equal access and declared compute. Incumbents remain accepted R2 and strong source controls. Source-only qualification is auxiliary/dev; it never substitutes for the five required Pets metrics or an independent Caltech result.

Problem-first contradiction: improving gallery membership/weight utility failed to deliver robust gains; whether per-image embedding variation carries useful information is untested. It is a hypothesis, not a diagnosed bottleneck.

False wins: multiple encodings versus an unreported one-view baseline; duplicate crops; extra labels; query-batch fitting; source-dev gains called independent confirmation; calling known tangent/augmentation methods novel. Local-only, zero spend, >=10GiB free, no downloads for this package, deadline 2026-09-15 09:00UTC. Cached features read-only.
