# Active experiment contract — real-view scatter

Selected idea-503deb34; decision-26360c22; branch run/representation-scatter-20260913 in existing workspace to preserve disk floor. Objective: test whether support-only augmentation scatter beats ordinary multiview aggregation under equal encoder access. H0: no robust gain beyond controls; H1: >=0.5pp both directional1shot macro gains with positive paired95%lower bounds; every cell lower bound >=-0.5pp. No novel tangent-invariance claim.

Tier auxiliary/dev; exact contract experiments/main/representation-scatter-20260913/protocol.json. Inputs: canonical source image identities/features and local encoders; both source domains are development-exposed. Six total views; original cache reused plus Resize256/FiveCrop224; same views for supports, query and gallery. No target labels, class names, encoder updates or cross-query fitting. Two paired transfer directions,1/5shot, matched/mismatched. Candidate gamma fixed grid chosen only on opposite source selection tasks. Full Pets metric contract remains untouched; source results are supplementary qualification only.

Minimum code: extract_views.py (reuse encoder), metric.py (low-rank scatter + existing R2), evaluate.py (reuse deterministic task sampler), verify.py (independent dense metric/permissions/parity audit). Freeze source hashes before real extraction; evaluator source separately locked before outcomes. Efficiency: MPSfloat32, float16cachedviews,6CPUloaders,32imagebatches, source-only extraction <=0.5GiB. No package changes.

Success: distinct crops and original-feature parity, complete14153image caches, finite normalized features, independent metric equivalence and query/class invariance checks, all source outcomes and auditable decision. Paired bootstrap byseed/episode conditional on fixed pools; no random-domain or held-out confirmation claim. Abandon/downgrade: crop duplicates, parity failure, resource floor, numerical failure, or failure of fixed source gain gate. Do not expand view/gamma grid after outcomes. Next if pass: freeze six-view metric variant then full Pets; if fail: record exact information-versus-geometry implication and return decision.

Primary evidence foundation: source-coverage factorial report-618dcdd5 and independent audit. Literature: representation_literature_survey.md; challenges: pre_idea_drafts/representation-scatter.md and cached-feature-augmentation.md. Root plan.md is the research map; only this workspace PLAN.md is the node contract (case-insensitive filesystem).

Execution amendment decision-9cd27e72: initial evaluation terminated before any complete target cell because floating mean tie-breaking violated the existing smallest-gamma rule. Compare integer correct counts (equal per-task query counts) and select the minimum tied gamma. Inputs,grid,permissions and acceptance gate unchanged. Preserve source outcomes and old lock under outputs/superseded_float_tie; regression verifies exact11282/15000tie chooses1, then rerun evaluation.
