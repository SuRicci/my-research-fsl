# Active experiment: learned aggregation order
Selected idea-aea8951a / decision-c173c093. Semantic idea/learned-aggregation-order-20260913 -> run/learned-aggregation-order-20260913; physical oslo-gallery-pets workspace reused to retain10GiBfloor. Parent view-distribution-kernel result is closed; prior scatter strict audit failure is retained.
Question/H1: can supervised nonlinear mapping before six-view averaging improve cross-domain classification over the identical map after averaging? H0: no both-direction robust gain. Established components, no method novelty claim.
Experiment tier: auxiliary/dev on exposed DTD/EuroSAT. Minimum: correct paired result and audit; solid: all frozen metric and seed gates; maximum: separately approved-in-protocol target variant and reserved confirmation. No target metrics this unit; all five Pets metric ids remain immutable.
Minimal code map: experiments/main/learned-aggregation-order-20260913/model.py for two orders; run.py for matched training/checkpoint selection and historical-bank references; verify_order.py for independent NumPy representation and intercept solver, gradients/invariants and saved output/statistic audits. No baseline edits.
Training: shared896->32->896residual ReLU(no bias), W2=0, same57344parameters.100source episodes, batches5,200updates, Adam0.001/weight_decay0.0001,3paired initialization/order seeds. Four source-only checkpoints0/50/100/200 ranked integer accuracy, CE,earliest. Identical source folds and13queries at5shot. Centered ridge0.1, queryCElogits×10. All24model choices locked before any evaluation outputs.
Evaluation: fixed2000unique tasks/eight galleryconditions. Store only two new methods' scores for3trainingseeds; reference14existing methods with exact hashes and task identity checks. No ensemble. Primary mean accuracy over trainingseeds; paired5000bootstrap stratified by taskseed, conditional uncertainty. Record each trainingseed effect.
Success: both1shotdirectionmacro gains>=0.5pp andpaired95%lower>0 over learned mean and every historical comparator; all shot/gallerycell95%lower>=-0.5pp;2/3seedspositiveagainstlearnedmeanbothdirections. Primary order contrast separate from generic head benefit. Failure is for this recipe, not all learnable representations.
Numerics: frozen independent-score tolerance3e-5; double formula1e-8; finite-difference gradient1e-6. Identity, linear commutation, duplicate-view collapse, view permutation, class equivariance, query chunk/order invariants; image fold separation. Audit all selected models on five fixed eval tasks plus three selection tasks. Parent scatter audit failure stays failed.
Costs: encoder frozen and caches reused; two methods have equal parameter/data/update budgets but different small-map FLOPs. Benchmark head timing separately. CPU6threads with BLAS2, existing torch env; no image/model downloads. Output<=0.10GiB,>=10GiBfree, zero spend, deadline2026-09-15T09:00Z. No duplicate worktree/data banks.
Abandon: metric/seedgate failure, unresolvednumeric error, disk/timefloor; no outcome-based architecture/grid rescue. Next: one meaningful implementationcheck->detached realrun->audit->auxiliaryreportanddecision. Remap source findings before future paper writing.
