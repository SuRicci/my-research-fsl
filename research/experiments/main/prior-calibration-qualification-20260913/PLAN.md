# Source-calibration qualification contract

Candidate idea-452b63b3, decision-68fe5761. Null: source-learned relevance does not improve paired target-domain accuracy beyond source-selected simple controls and equal-mass shrinkage. Alternative: learned composition transfers usefully. This is auxiliary/dev qualification using exposed DTD and EuroSAT; no target confirmation claim.

Source labels: train the calibrator on one domain only, using the first deterministic half of each class query pool as support and that source gallery; all compared configurations share the same source-label allowance. Source control selection uses the other half for independent within-task support/query samples. Target domain is never used to fit or select that fold. Roles reverse for the second fold. Semantic independence of pretraining is not asserted.

Exact protocol is protocol.json. One-shot relevance features: max similarity and top-two gap for CLIP, DINO and fused prototypes, plus encoder argmax agreement. Balanced logistic C1 produces a posterior under reference prior0.5. Maximum-likelihood binary prior EM sees all gallery scores only. Retrieve the same top64 as R2; posterior-weight the neighbor mean and shrink R2 mix0.5 by mean posterior. Compare fixed prior, composition-only and same-mass uniform-neighbor controls. Five-shot returns unchanged R2; no five-shot innovation claimed. Strong references include R2, CS_l2, support logistic C1/C10 and source-selected mix/lambda controls.

Minimum evidence: exact unit/inference checks and complete two-direction source transfer; solid: paired macro CI above zero in each direction against source-selected control, no cell loss>0.5pp, and composition beats same-mass control. Failure closes the source-transfer premise without a target grid. Caltech source is qualified but images remain untouched until success. Its download/RGB checks and separately named comparator remain pending.

Minimal code map: calibrate.py for hypothesis-bound operators; qualify.py for existing feature loading, folds, task outputs and paired analysis. Frozen source implementations remain read-only. One useful smoke maximum unless a real fix requires a second. All scientific execution uses existing torch Conda interpreter and managed bash sessions. CPU6, batch8, zero spend, free>=10GiB; no new checkout/data/model needed. Bootstrap is paired and stratified by task seed, conditional on fixed pools; it does not prove independent-domain population coverage.

The five canonical Pets metric ids remain unchanged in the accepted baseline contract. They are not fabricated for this auxiliary gate. A failed source qualification is recorded as a report/science result and decision; a surviving candidate proceeds to a complete main/test Pets record, then a separately scoped Caltech confirmation. Current paper remains a retained checkpoint. Physical workspace is reused on a dedicated run branch from its paper documentation checkpoint to avoid duplicate binaries; source experiment remains its measured ancestor.
