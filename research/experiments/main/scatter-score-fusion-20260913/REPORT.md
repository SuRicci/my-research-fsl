# Fixed prediction fusion: a small verified increment

A single equal average of scatterCS and scatterR2 scores reaches77.334% pooled one-shot accuracy, versus77.188% for the retained stack and76.968% for scatterR2. The incremental effect versus the stack is+0.146pp, paired95%CI[0.05998,0.23268]. This is positive but below the frozen0.5pp threshold, so the promotion gate is false. No threshold, weight, temperature, normalization or target-specific expert choice changed.

Domain effects versus the stack: EuroSAT+0.11733pp[-0.02533,0.25737]; DTD+0.17467pp[0.07600,0.27733]. Both domain means are nonnegative and all cell noninferiority checks pass. Versus scatterR2, the pooled effect is+0.366pp[0.28267,0.44800] and the declared gate against that comparator passes. Versus the older meanCS reference, the cumulative effect is+1.794pp[1.60265,1.98667]. The record retains this small gain without treating it as the stronger prespecified research result.

Selectedidea-f6de44e6; same six-view frozen features, inherited source gamma, same1000 one-shot tasks and two gallery conditions. No five-shot method/result changed and no newPets/Caltech outcome was measured. This remains repeated exposed-development evidence, with intervals conditional on fixed image pools. Prediction averaging is established and no algorithm novelty is claimed.

The complete run bash-ab3c3e0e took48.110s. All4000 parent endpoint task checks matched,20 sampled mixtures matched independently recomputed dense NumPy experts and predictions, maximum error9.7561e-15. All scores sum toone, which verifies the shared label-score origin but does not imply calibrated probabilities. Full task predictions and means are saved; intervals use the parent's already independently audited bootstrap helper. No additional training or downloads; localCPU, zero extra spend.

Decision recommendation: retain77.334% as the highest measured conditional one-shot candidate, with77.188% and76.968% as required component comparators. Do not promote to new target evaluation under the failed0.5pp gate or open a weight/temperature sweep. Close the fixed-fusion attempt. The next idea pass should examine residual failure structure and a different compatible mechanism, including whether the common scatter model loses useful class-specific variation, before selecting another experiment. This is a candidate question, not a new accepted method. Earlier source-trained geometry regularization remains an outside-family alternative with known transfer/identity risks. Preserve the complete positive and negative history.

Evidence: protocol.json, code_lock.json, study.py, outputs/manifest.json, analysis.json, validation.json, complete.json and fourNPZ prediction banks. The previous study and attribution remain immutable. Future paper work must first reconcile scope and map relevant auxiliary evidence; no submission-readiness claim follows from these checks.
