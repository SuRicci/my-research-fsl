# Active run contract: r2-ownership-canonical-20260912
Selected idea idea-5670808c. Primary question: does exclusive ownership of original top64 gallery candidates improve one-shot classification beyond R2, CS_l2, and matched count/mass controls? Null: it does not, or gains are explained by sample count/weight. Alternative: positive practically relevant macro gain remains after these controls. Research type: controlled incremental method intervention and attribution, not novelty confirmation.

## Deliverable and code map
Reuse reference_eval.py assets/tasks and baseline_methods.py ridge; preserve read-only historical sources. Add evaluate.py for one ownership mask and matched controls, validate_implementation.py for a bounded independent check, package_results.py for paired evidence. Copy geometry evaluator as geometry_reference.py only to preserve existing CS_l2 comparator behavior.

## Protocol
Use exact accepted feature identities and dtd-eurosat-canonical-locked-20260912 scope, same four one-shot conditions and two five-shot conditions, all seven canonical metrics. Original DTD development classes/seeds/tasks only; each readout same four ridge lambdas, fixed half mix and top64. Five-shot primary inherits raw R2 unchanged. See ownership_idea.md and protocol.json for formulas and controls.
No query-set adaptation, query/gallery labels in adaptation, training encoders, class text, model changes, downloads or remote execution. All pools already viewed. CPU6threads and batch16 reuse features; no assumption that current six threads is a user restriction.

## Evidence targets and statistics
Minimum: finite complete six-cell paired outputs, all7metrics, endpoint and permission validation.
Solid: 5000 within-seed paired task bootstraps preserving shared-gallery pairing; primary macro >=0.2pp with positive lower95%CI vs both R2 and CS_l2, every one-shot cell point loss<=0.5pp versus either; positive paired macro lower bounds vs nearest-count/random-count/mass-only for ownership attribution.
Maximum only after success: fixed-method alternate-pool transfer and stronger gallery-only cleaning comparator. Not a prerequisite for reporting this main run.

## Abandonment
Any unmet gate rejects promotion. Do not select secondary centered ownership or filter-plus-mass post hoc as a replacement winner. No threshold/refill tuning on evaluation pools. If implementation invalid, stop, fix the concrete issue and rerun once; otherwise retain negative findings and route through decision.
Stop resource pressure before free disk<10GiB; first-round deadline remains2026-09-15 09:00UTC.

## Middle tasks
1. Implement and validate ownership, empty-mask fallback, query independence, class permutation and reference endpoints.
2. Run development selection then the full measured evaluation in one managed process; preserve logs and hashes.
3. Validate saved arrays/required metrics, record main experiment and interpret through decision.

## Exit state
Completed: six paired cells and7requiredmetrics validated, ownership gate failed. Main result and decision-61eadffc are durable. Primary macro72.785%; deltaR2+0.011667ppCI[-0.115333,0.138342], deltaCS_l2-0.348333ppCI[-0.477667,-0.221992]. Next: inspect iLPC gallery-only source and cost under analysis-campaign; do not repeat ownership tuning.
