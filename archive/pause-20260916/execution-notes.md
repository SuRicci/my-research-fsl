# Archive execution notes
Initial secret scan matched the suffix `sk-` inside long `task-*` research names. Redacted inspection showed name/path false positives. Added an identifier boundary; rerun still scans all text blobs and blocks real credential patterns.
Full default clone was superseded by a filtered no-checkout clone to avoid redownloading already archived large research assets for a report-only main update.

First Git push failed HTTP400 while sending1.17MiB pack; local clone transport changed to bufferedHTTP/1.1, existing commit retained.
